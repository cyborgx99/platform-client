import { Form } from 'formik';
import styled from 'styled-components';
import { HeaderThreeBase } from 'styles/globalStyles';

export const StyledForm = styled(Form)`
  padding: 0.5rem;
`;

export const HeaderThree = styled(HeaderThreeBase)`
  margin: 1rem 0;
`;
